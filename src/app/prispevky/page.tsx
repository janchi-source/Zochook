
// src/app/posts/page.tsx

import Typography from "@mui/material/Typography";
import Container from "@mui/material/Container";

export const metadata = {title: "Listovanie postov | Kolcobos"};

export default function ListingPosts() {
  return (
    <Container>
      <Typography> Listing postov </Typography>
    </Container>
  );
}